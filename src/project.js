const project = (() => {
  let toDoItems = {
    title: 'title',
    description: 'description', 
    priority: 'priority',
    notes: 'notes',
    checklist: false,
    dueDate: 'duedate'
  }
  return toDoItems;
})();

export default 'project'